---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.19.0
  name: siteuserpermissions.vyogo.tech
spec:
  group: vyogo.tech
  names:
    kind: SiteUserPermission
    listKind: SiteUserPermissionList
    plural: siteuserpermissions
    singular: siteuserpermission
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .spec.siteRef.name
      name: Site
      type: string
    - jsonPath: .spec.user
      name: User
      type: string
    - jsonPath: .spec.allow
      name: Allow
      type: string
    - jsonPath: .spec.forValue
      name: For Value
      type: string
    - jsonPath: .status.phase
      name: Status
      type: string
    - jsonPath: .metadata.creationTimestamp
      name: Age
      type: date
    name: v1
    schema:
      openAPIV3Schema:
        description: SiteUserPermission is the Schema for the siteuserpermissions
          API
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: SiteUserPermissionSpec defines the desired state of SiteUserPermission
            properties:
              allow:
                description: Allow specifies the DocType to restrict (e.g. "Company",
                  "Warehouse", "Territory").
                minLength: 1
                type: string
              applyToAllDoctypes:
                default: true
                description: ApplyToAllDocTypes specifies whether this restriction
                  applies across all linked DocTypes. Defaults to true.
                type: boolean
              forValue:
                description: ForValue specifies the target document ID/value to allow
                  (e.g. "Acme Corp", "Main Warehouse").
                minLength: 1
                type: string
              siteRef:
                description: SiteRef specifies the target FrappeSite custom resource.
                properties:
                  name:
                    description: Name of the resource
                    type: string
                  namespace:
                    description: Namespace of the resource
                    type: string
                required:
                - name
                type: object
              user:
                description: User specifies the target Frappe user email (e.g. "john.doe@example.com").
                minLength: 1
                type: string
            required:
            - allow
            - forValue
            - siteRef
            - user
            type: object
          status:
            description: SiteUserPermissionStatus defines the observed state of SiteUserPermission
            properties:
              conditions:
                description: Conditions represents the latest available observations
                  of the resource state.
                items:
                  description: Condition contains details for one aspect of the current
                    state of this API Resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        lastTransitionTime is the last time the condition transitioned from one status to another.
                        This should be when the underlying condition changed.  If that is not known, then using the time when the API field changed is acceptable.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        message is a human readable message indicating details about the transition.
                        This may be an empty string.
                      maxLength: 32768
                      type: string
                    observedGeneration:
                      description: |-
                        observedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      minimum: 0
                      type: integer
                    reason:
                      description: |-
                        reason contains a programmatic identifier indicating the reason for the condition's last transition.
                        Producers of specific condition types may define expected values and meanings for this field,
                        and whether the values are considered a guaranteed API.
                        The value should be a CamelCase string.
                        This field may not be empty.
                      maxLength: 1024
                      minLength: 1
                      pattern: ^[A-Za-z]([A-Za-z0-9_,:]*[A-Za-z0-9_])?$
                      type: string
                    status:
                      description: status of the condition, one of True, False, Unknown.
                      enum:
                      - "True"
                      - "False"
                      - Unknown
                      type: string
                    type:
                      description: type of condition in CamelCase or in foo.example.com/CamelCase.
                      maxLength: 316
                      pattern: ^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$
                      type: string
                  required:
                  - lastTransitionTime
                  - message
                  - reason
                  - status
                  - type
                  type: object
                type: array
              observedGeneration:
                description: ObservedGeneration is the most recent generation observed
                  by the controller.
                format: int64
                type: integer
              phase:
                description: Phase represents the current state of the SiteUserPermission
                  (Pending, Ready, Failed).
                type: string
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
