---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.19.0
  name: siteconfigs.vyogo.tech
spec:
  group: vyogo.tech
  names:
    kind: SiteConfig
    listKind: SiteConfigList
    plural: siteconfigs
    singular: siteconfig
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .status.phase
      name: Phase
      type: string
    - jsonPath: .spec.siteRef.name
      name: Site
      type: string
    - jsonPath: .metadata.creationTimestamp
      name: Age
      type: date
    name: v1
    schema:
      openAPIV3Schema:
        description: SiteConfig is the Schema for the siteconfigs API
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: SiteConfigSpec defines the desired state of SiteConfig
            properties:
              customConfig:
                additionalProperties:
                  type: string
                description: |-
                  CustomConfig defines arbitrary key-value pairs to set in site_config.json.
                  Values that parse as JSON (objects, arrays, numbers, booleans) are applied as such;
                  everything else is applied as a string.
                type: object
              encryptionKeySecretRef:
                description: EncryptionKeySecretRef specifies a K8s secret holding
                  an encryption_key to inject into site_config.json.
                properties:
                  name:
                    default: ""
                    description: |-
                      Name of the referent.
                      This field is effectively required, but due to backwards compatibility is
                      allowed to be empty. Instances of this type with an empty value here are
                      almost certainly wrong.
                      More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#names
                    type: string
                type: object
                x-kubernetes-map-type: atomic
              maintenanceMode:
                description: MaintenanceMode determines whether maintenance mode is
                  enabled for the site.
                type: boolean
              maxFileSize:
                description: MaxFileSize specifies the maximum upload file size in
                  bytes (max_file_size in site_config.json).
                format: int64
                type: integer
              objectStorage:
                description: |-
                  ObjectStorage, when set, configures the site to offload File attachments to an
                  S3-compatible object store via the cloud_storage app (baked into the bench image).
                  The operator assembles the cloud_storage_settings block in site_config.json, sourcing
                  the access key/secret from CredentialsSecretRef so they never appear in the CR.
                properties:
                  accessKeyKey:
                    description: AccessKeyKey is the Secret key holding the access
                      key. Defaults to "access_key".
                    type: string
                  bucket:
                    description: Bucket is the S3 bucket name.
                    type: string
                  credentialsSecretRef:
                    description: CredentialsSecretRef references a Secret holding
                      the S3 credentials.
                    properties:
                      name:
                        default: ""
                        description: |-
                          Name of the referent.
                          This field is effectively required, but due to backwards compatibility is
                          allowed to be empty. Instances of this type with an empty value here are
                          almost certainly wrong.
                          More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#names
                        type: string
                    type: object
                    x-kubernetes-map-type: atomic
                  endpointUrl:
                    description: EndpointURL is the S3 endpoint (e.g. "https://s3.amazonaws.com",
                      "http://minio:9000").
                    type: string
                  folder:
                    description: |-
                      Folder is the key prefix within the bucket. Defaults to the site name, giving each
                      site its own prefix when a bucket is shared across tenants.
                    type: string
                  presignedUrlExpirySeconds:
                    description: |-
                      PresignedURLExpirySeconds is the TTL for presigned download URLs (cloud_storage
                      "expiration"). Defaults to 120.
                    format: int64
                    type: integer
                  region:
                    description: Region is the S3 region (e.g. "us-east-1").
                    type: string
                  secretKeyKey:
                    description: SecretKeyKey is the Secret key holding the secret
                      key. Defaults to "secret".
                    type: string
                required:
                - bucket
                - credentialsSecretRef
                - endpointUrl
                type: object
              siteRef:
                description: SiteRef specifies the target FrappeSite custom resource.
                properties:
                  name:
                    description: Name of the resource
                    type: string
                  namespace:
                    description: Namespace of the resource
                    type: string
                required:
                - name
                type: object
            required:
            - siteRef
            type: object
          status:
            description: SiteConfigStatus defines the observed state of SiteConfig
            properties:
              appliedKeys:
                description: AppliedKeys lists the configuration keys successfully
                  merged into site_config.json.
                items:
                  type: string
                type: array
              conditions:
                description: Conditions represent the latest available observations
                  of the SiteConfig's state.
                items:
                  description: Condition contains details for one aspect of the current
                    state of this API Resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        lastTransitionTime is the last time the condition transitioned from one status to another.
                        This should be when the underlying condition changed.  If that is not known, then using the time when the API field changed is acceptable.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        message is a human readable message indicating details about the transition.
                        This may be an empty string.
                      maxLength: 32768
                      type: string
                    observedGeneration:
                      description: |-
                        observedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      minimum: 0
                      type: integer
                    reason:
                      description: |-
                        reason contains a programmatic identifier indicating the reason for the condition's last transition.
                        Producers of specific condition types may define expected values and meanings for this field,
                        and whether the values are considered a guaranteed API.
                        The value should be a CamelCase string.
                        This field may not be empty.
                      maxLength: 1024
                      minLength: 1
                      pattern: ^[A-Za-z]([A-Za-z0-9_,:]*[A-Za-z0-9_])?$
                      type: string
                    status:
                      description: status of the condition, one of True, False, Unknown.
                      enum:
                      - "True"
                      - "False"
                      - Unknown
                      type: string
                    type:
                      description: type of condition in CamelCase or in foo.example.com/CamelCase.
                      maxLength: 316
                      pattern: ^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$
                      type: string
                  required:
                  - lastTransitionTime
                  - message
                  - reason
                  - status
                  - type
                  type: object
                type: array
              observedGeneration:
                description: ObservedGeneration is the most recent generation observed
                  by the controller.
                format: int64
                type: integer
              phase:
                description: Phase represents the current state of the SiteConfig
                  (Pending, Ready, Failed).
                type: string
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
