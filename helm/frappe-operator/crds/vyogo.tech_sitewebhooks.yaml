---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.19.0
  name: sitewebhooks.vyogo.tech
spec:
  group: vyogo.tech
  names:
    kind: SiteWebhook
    listKind: SiteWebhookList
    plural: sitewebhooks
    singular: sitewebhook
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .spec.siteRef.name
      name: Site
      type: string
    - jsonPath: .spec.webhookDoctype
      name: DocType
      type: string
    - jsonPath: .spec.webhookEvent
      name: Event
      type: string
    - jsonPath: .spec.requestUrl
      name: URL
      type: string
    - jsonPath: .status.phase
      name: Status
      type: string
    - jsonPath: .metadata.creationTimestamp
      name: Age
      type: date
    name: v1
    schema:
      openAPIV3Schema:
        description: SiteWebhook is the Schema for the sitewebhooks API
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: SiteWebhookSpec defines the desired state of SiteWebhook
            properties:
              enabled:
                default: true
                description: Enabled specifies whether the webhook is enabled. Defaults
                  to true.
                type: boolean
              requestStructure:
                default: JSON
                description: RequestStructure specifies the HTTP request content type
                  ("JSON", "Form URL-Encoded"). Defaults to "JSON".
                type: string
              requestUrl:
                description: RequestURL specifies the target endpoint URL to post
                  webhook payloads.
                type: string
              siteRef:
                description: SiteRef specifies the target FrappeSite custom resource.
                properties:
                  name:
                    description: Name of the resource
                    type: string
                  namespace:
                    description: Namespace of the resource
                    type: string
                required:
                - name
                type: object
              webhookDoctype:
                description: WebhookDocType specifies the DocType that triggers this
                  webhook (e.g. "Sales Order", "Customer").
                minLength: 1
                type: string
              webhookEvent:
                description: WebhookEvent specifies the document event ("on_change",
                  "on_update", "on_submit", "on_cancel", "on_trash").
                enum:
                - on_change
                - on_update
                - on_submit
                - on_cancel
                - on_trash
                type: string
            required:
            - requestUrl
            - siteRef
            - webhookDoctype
            - webhookEvent
            type: object
          status:
            description: SiteWebhookStatus defines the observed state of SiteWebhook
            properties:
              conditions:
                description: Conditions represents the latest available observations
                  of the resource state.
                items:
                  description: Condition contains details for one aspect of the current
                    state of this API Resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        lastTransitionTime is the last time the condition transitioned from one status to another.
                        This should be when the underlying condition changed.  If that is not known, then using the time when the API field changed is acceptable.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        message is a human readable message indicating details about the transition.
                        This may be an empty string.
                      maxLength: 32768
                      type: string
                    observedGeneration:
                      description: |-
                        observedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      minimum: 0
                      type: integer
                    reason:
                      description: |-
                        reason contains a programmatic identifier indicating the reason for the condition's last transition.
                        Producers of specific condition types may define expected values and meanings for this field,
                        and whether the values are considered a guaranteed API.
                        The value should be a CamelCase string.
                        This field may not be empty.
                      maxLength: 1024
                      minLength: 1
                      pattern: ^[A-Za-z]([A-Za-z0-9_,:]*[A-Za-z0-9_])?$
                      type: string
                    status:
                      description: status of the condition, one of True, False, Unknown.
                      enum:
                      - "True"
                      - "False"
                      - Unknown
                      type: string
                    type:
                      description: type of condition in CamelCase or in foo.example.com/CamelCase.
                      maxLength: 316
                      pattern: ^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$
                      type: string
                  required:
                  - lastTransitionTime
                  - message
                  - reason
                  - status
                  - type
                  type: object
                type: array
              observedGeneration:
                description: ObservedGeneration is the most recent generation observed
                  by the controller.
                format: int64
                type: integer
              phase:
                description: Phase represents the current state of the SiteWebhook
                  (Pending, Ready, Failed).
                type: string
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
