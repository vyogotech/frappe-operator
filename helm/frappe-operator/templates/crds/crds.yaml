{{/* 
CRDs are installed from the crds/ directory at the chart root.
This template file exists to indicate CRD handling in the chart.
Helm will automatically install any YAML files in the crds/ directory.
*/}}

