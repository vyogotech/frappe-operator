{{- if .Values.rbac.create -}}
apiVersion: rbac.authorization.k8s.io/v1
kind: ClusterRoleBinding
metadata:
  name: {{ include "frappe-operator.fullname" . }}-manager-rolebinding
  labels:
    {{- include "frappe-operator.labels" . | nindent 4 }}
roleRef:
  apiGroup: rbac.authorization.k8s.io
  kind: ClusterRole
  name: {{ include "frappe-operator.fullname" . }}-manager-role
subjects:
- kind: ServiceAccount
  name: {{ include "frappe-operator.serviceAccountName" . }}
  namespace: {{ include "frappe-operator.namespace" . }}
{{- end }}

