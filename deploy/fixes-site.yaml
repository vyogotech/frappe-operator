apiVersion: vyogo.tech/v1alpha1
kind: FrappeSite
metadata:
  name: fixes-site5
  namespace: frappe-fixes
spec:
  siteName: fixes5-site.apps.cluster-dz66n.dynamic.redhatworkshops.io
  benchRef:
    name: fixes-bench

  # Database configuration
  dbConfig:
    provider: mariadb
    mode: shared
    mariadbRef:
      name: frappe-mariadb
      namespace: frappe-operator-system

  # Ingress configuration for OpenShift
  ingress:
    enabled: true
    className: openshift-default
  

