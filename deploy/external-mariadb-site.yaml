apiVersion: vyogo.tech/v1
kind: FrappeSite
metadata:
  name: external-mock-site
  namespace: frappe
spec:
  benchRef:
    name: autoscaling-hpa-bench
  siteName: external-mock.apps.crc.testing
  adminPasswordSecretRef:
    name: external-mock-site-admin-password
  dbConfig:
    provider: external
    host: 64.227.178.150
    port: "3306"
    connectionSecretRef:
      name: external-mariadb-creds