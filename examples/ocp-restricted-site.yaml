apiVersion: vyogo.tech/v1
kind: FrappeSite
metadata:
  name: ocp-restricted-site
  namespace: frappe-test
spec:
  benchRef:
    name: ocp-restricted-bench
    namespace: frappe-test
  siteName: test.example.com
  adminPasswordSecretRef:
    name: ocp-restricted-site-admin-password
  dbConfig:
    mode: dedicated
    provider: mariadb
    image: quay.io/bitnami/mariadb:10.11
