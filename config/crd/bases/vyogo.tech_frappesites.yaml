---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.14.0
  name: frappesites.vyogo.tech
spec:
  group: vyogo.tech
  names:
    kind: FrappeSite
    listKind: FrappeSiteList
    plural: frappesites
    singular: frappesite
  scope: Namespaced
  versions:
  - name: v1alpha1
    schema:
      openAPIV3Schema:
        description: FrappeSite is the Schema for the frappesites API
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: FrappeSiteSpec defines the desired state of FrappeSite
            properties:
              adminPasswordSecretRef:
                description: AdminPasswordSecretRef references the Secret containing
                  admin password
                properties:
                  name:
                    description: name is unique within a namespace to reference a
                      secret resource.
                    type: string
                  namespace:
                    description: namespace defines the space within which the secret
                      name must be unique.
                    type: string
                type: object
                x-kubernetes-map-type: atomic
              benchRef:
                description: BenchRef references the FrappeBench this site belongs
                  to
                properties:
                  name:
                    description: Name of the resource
                    type: string
                  namespace:
                    description: Namespace of the resource
                    type: string
                required:
                - name
                type: object
              dbConfig:
                description: DBConfig defines database configuration for this site
                properties:
                  connectionSecretRef:
                    description: ConnectionSecretRef for external database
                    properties:
                      name:
                        description: name is unique within a namespace to reference
                          a secret resource.
                        type: string
                      namespace:
                        description: namespace defines the space within which the
                          secret name must be unique.
                        type: string
                    type: object
                    x-kubernetes-map-type: atomic
                  mariadbRef:
                    description: MariadbRef references an existing MariaDB cluster
                    properties:
                      name:
                        description: Name of the resource
                        type: string
                      namespace:
                        description: Namespace of the resource
                        type: string
                    required:
                    - name
                    type: object
                  mode:
                    description: 'Mode: shared, dedicated, or external'
                    enum:
                    - shared
                    - dedicated
                    - external
                    type: string
                  resources:
                    description: Resources for dedicated MariaDB
                    properties:
                      limits:
                        additionalProperties:
                          anyOf:
                          - type: integer
                          - type: string
                          pattern: ^(\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))(([KMGTPE]i)|[numkMGTPE]|([eE](\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))))?$
                          x-kubernetes-int-or-string: true
                        description: Limits describes the maximum amount of compute
                          resources allowed
                        type: object
                      requests:
                        additionalProperties:
                          anyOf:
                          - type: integer
                          - type: string
                          pattern: ^(\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))(([KMGTPE]i)|[numkMGTPE]|([eE](\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))))?$
                          x-kubernetes-int-or-string: true
                        description: Requests describes the minimum amount of compute
                          resources required
                        type: object
                    type: object
                  storageSize:
                    anyOf:
                    - type: integer
                    - type: string
                    description: StorageSize for dedicated MariaDB
                    pattern: ^(\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))(([KMGTPE]i)|[numkMGTPE]|([eE](\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))))?$
                    x-kubernetes-int-or-string: true
                required:
                - mode
                type: object
              domain:
                description: |-
                  Domain is the external domain for ingress
                  MUST match siteName (defaults to siteName if not specified)
                type: string
              ingress:
                description: Ingress configuration
                properties:
                  annotations:
                    additionalProperties:
                      type: string
                    description: Annotations for the Ingress
                    type: object
                  className:
                    description: ClassName is the Ingress class
                    type: string
                  enabled:
                    description: Enabled controls whether to create Ingress
                    type: boolean
                  tls:
                    description: TLS configuration
                    properties:
                      certManagerIssuer:
                        description: CertManagerIssuer is the cert-manager ClusterIssuer
                          name
                        type: string
                      enabled:
                        description: Enabled enables TLS
                        type: boolean
                      issuer:
                        description: Issuer is an alias for CertManagerIssuer (deprecated,
                          use CertManagerIssuer)
                        type: string
                      secretName:
                        description: SecretName is the name of the TLS secret
                        type: string
                    type: object
                type: object
              ingressClassName:
                description: IngressClassName specifies the ingress class
                type: string
              siteName:
                description: |-
                  SiteName is the Frappe site name - MUST match the domain that will receive traffic
                  This is what Frappe uses to route requests based on HTTP Host header
                  Example: "erp.customer.com" or "customer1.myplatform.com"
                pattern: ^[a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*$
                type: string
              tls:
                description: TLS configuration
                properties:
                  certManagerIssuer:
                    description: CertManagerIssuer is the cert-manager ClusterIssuer
                      name
                    type: string
                  enabled:
                    description: Enabled enables TLS
                    type: boolean
                  issuer:
                    description: Issuer is an alias for CertManagerIssuer (deprecated,
                      use CertManagerIssuer)
                    type: string
                  secretName:
                    description: SecretName is the name of the TLS secret
                    type: string
                type: object
            required:
            - benchRef
            - siteName
            type: object
          status:
            description: FrappeSiteStatus defines the observed state of FrappeSite
            properties:
              benchReady:
                description: BenchReady indicates if the referenced bench is ready
                type: boolean
              dbConnectionSecret:
                description: DBConnectionSecret is the name of the Secret with DB
                  credentials
                type: string
              domainSource:
                description: |-
                  DomainSource indicates how domain was determined
                  Values: explicit, bench-suffix, auto-detected, sitename-default
                type: string
              phase:
                description: Phase is the current phase
                type: string
              resolvedDomain:
                description: ResolvedDomain is the final domain after resolution
                type: string
              siteURL:
                description: SiteURL is the accessible URL
                type: string
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
