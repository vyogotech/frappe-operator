---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.19.0
  name: sitebackups.vyogo.tech
spec:
  group: vyogo.tech
  names:
    kind: SiteBackup
    listKind: SiteBackupList
    plural: sitebackups
    singular: sitebackup
  scope: Namespaced
  versions:
  - name: v1alpha1
    schema:
      openAPIV3Schema:
        description: SiteBackup is the Schema for the sitebackups API
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: SiteBackupSpec defines the desired state of SiteBackup
            properties:
              backupPath:
                description: |-
                  BackupPath specifies the path to save the backup files
                  If empty, uses the default site backup location
                type: string
              backupPathConf:
                description: BackupPathConf specifies the path to save the config
                  file
                type: string
              backupPathDB:
                description: BackupPathDB specifies the path to save the database
                  file
                type: string
              backupPathFiles:
                description: BackupPathFiles specifies the path to save the public
                  file
                type: string
              backupPathPrivateFiles:
                description: BackupPathPrivateFiles specifies the path to save the
                  private file
                type: string
              compress:
                default: false
                description: Compress compresses the backup files
                type: boolean
              exclude:
                description: Exclude specifies the DocTypes to not backup, separated
                  by commas
                items:
                  type: string
                type: array
              ignoreBackupConf:
                default: false
                description: IgnoreBackupConf ignores excludes/includes set in config
                type: boolean
              include:
                description: Include specifies the DocTypes to backup, separated by
                  commas
                items:
                  type: string
                type: array
              schedule:
                description: |-
                  Schedule is a cron expression for scheduled backups (e.g., "0 2 * * *")
                  If empty, performs a one-time backup
                type: string
              site:
                description: Site is the name of the Frappe site to backup
                type: string
              verbose:
                default: false
                description: Verbose adds verbosity to the backup process
                type: boolean
              withFiles:
                default: false
                description: WithFiles includes private and public files in the backup
                type: boolean
            required:
            - site
            type: object
          status:
            description: SiteBackupStatus defines the observed state of SiteBackup
            properties:
              lastBackup:
                description: LastBackup is the timestamp of the last successful backup
                format: date-time
                type: string
              lastBackupJob:
                description: LastBackupJob is the name of the last backup job or cronjob
                type: string
              message:
                description: Message provides additional information about the backup
                  status
                type: string
              phase:
                description: Phase indicates the current phase of the backup
                type: string
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
