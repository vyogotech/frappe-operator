apiVersion: v1
kind: Service
metadata:
  name: openclaw-dashboard
  namespace: sre-bot-system
spec:
  selector:
    app: openclaw-bot
  ports:
    - protocol: TCP
      port: 18789
      targetPort: 18789
  type: ClusterIP
